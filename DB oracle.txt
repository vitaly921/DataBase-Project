1. suschnost Installation

CREATE TABLE  "INSTALL" 
   (	"ID" NUMBER, 
	"COST" NUMBER, 
	"INSTDATE" DATE, 
	"LAUNCHTIME" NUMBER, 
	"PURPOSEOFUSE" VARCHAR2(140), 
	 PRIMARY KEY ("ID")
  USING INDEX  ENABLE
   )
/

2. suschnost GTU

CREATE TABLE  "GTU" 
   (	"ID" NUMBER, 
	"TYPE" VARCHAR2(140), 
	"BLOKS" NUMBER, 
	"DEGREE_AGREGATION" VARCHAR2(140), 
	"OPTIONAL_EQUIPMENT" VARCHAR2(140), 
	 PRIMARY KEY ("ID")
  USING INDEX  ENABLE
   )
/

3. suschnost Foundation

CREATE TABLE  "FOUNDAT" 
   (	"ID" NUMBER, 
	"TYPE_FOUNDATION" VARCHAR2(140), 
	"THE_SIZE" NUMBER, 
	"NUM_BLOCKS" NUMBER, 
	"NUM_ANCHOR_STUDS" NUMBER, 
	 PRIMARY KEY ("ID")
  USING INDEX  ENABLE
   )
/

4. suschnost Cable

CREATE TABLE  "CABLE" 
   (	"ID" NUMBER, 
	"TYPE_CABLE" VARCHAR2(140), 
	"TYPE_CONNECTION" VARCHAR2(140), 
	"NUM_CONNECT" NUMBER, 
	"DESIGN_TRAY" VARCHAR2(140), 
	 PRIMARY KEY ("ID")
  USING INDEX  ENABLE
   )
/
 